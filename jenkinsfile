pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        
        stage('Deploy') {
            steps {
                sh 'cp target/petclinic-0.0.1-SNAPSHOT.jar /home/ubuntu/webapps/'
            }
        }
    }
}
