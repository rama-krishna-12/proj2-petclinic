pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Deploy to Tomcat') {
            steps {
                sh 'sudo cp target/petclinic-0.0.1-SNAPSHOT.jar /opt/tomcat/webapps/'
            }
        }
    }
}
